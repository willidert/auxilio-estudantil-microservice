syntax = "proto3";

service ModelService {
  rpc MakePrediction(Features) returns (PredictionResponse) {}
}

message Features {
  repeated int32 features = 1;
}

message PredictionResponse {
  int32 prediction = 1;
}
